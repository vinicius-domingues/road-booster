# Atualizar Tudo

Copie o prompt abaixo e envie para a IA para realizar uma manutenção completa na plataforma.

---
**PROMPT:**

Aja como um Tech Lead e revise completamente a estrutura de dados do arquivo `road-booster-center/script.js`.

**Instruções de Atualização Completa:**
1. **Vagas (`jobRoles`):** Identifique tendências recentes de mercado. Crie 3 novas vagas de alta demanda que não estão no array atual.
2. **Trilha (`phases` / `roadmaps`):** Baseado nas novas vagas incluídas no passo anterior, identifique se alguma tecnologia demandada está faltando na trilha atual (ex: um novo banco de dados em alta, uma nova ferramenta cloud).
3. Crie os novos tópicos (`topics`) correspondentes nas fases adequadas com seus `tools`, `sites` e `resources`.
4. Respeite o padrão de objetos já existente em ambas as variáveis.
5. Retorne os trechos de código exatos que eu devo substituir em `script.js` (array `jobRoles` novo e os `topics` novos).
